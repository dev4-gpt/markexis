# Copy to scrapers/.env (gitignored). The scrapers read these at runtime.
N8N_WEBHOOK_URL=http://67.207.89.85:5678/webhook/lead-ingest

# Optional: enable POST during `node index.js --selftest`
# SELFTEST_POST=1
